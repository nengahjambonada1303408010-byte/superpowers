# Requirements untuk Termux / Android
# Tidak ada uvicorn[standard] — menghindari kompilasi C berat di Android
#
# Install dengan:
#   pip install -r requirements-termux.txt --prefer-binary
# Atau jalankan: bash termux/setup.sh  (sudah otomatis)

anthropic>=0.49.0
fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.28.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
python-multipart>=0.0.20
